<h2 class="mt-4">Get Transaction Files</h2>

<%= button_to 'Send Request', fetch_transactions_transactions_path, method: :post, class: 'btn btn-primary mt-3' %>

<!-- This will hold the list of files -->
<div id="transaction_list">
  <%= render partial: 'list', locals: { files: @files } %>
</div>
